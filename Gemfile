source "https://rubygems.org"

# Use the github-pages gem so local builds match what GitHub Pages runs.
gem "github-pages", group: :jekyll_plugins

# Optional: faster local rebuilds
gem "webrick", "~> 1.8"
